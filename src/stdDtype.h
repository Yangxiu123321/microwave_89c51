#ifndef __STD_DTYPE_H
#define __STD_DTYPE_H

typedef unsigned char uint8_t;
typedef unsigned int uint16_t;

typedef enum {RESET = 0, SET = 1} FlagStatus, ITStatus;
//使能失能状态枚举类型定义
typedef enum {disable = 0, enable} FunctionalState_t;

#endif